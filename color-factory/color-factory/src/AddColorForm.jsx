import React from 'react';

const AddColorForm = () => {
    // Handle form submission and add new color

    return (
        <div>
            <h1>Add New Color</h1>
            <form>
                {/* Add form fields for color name and hex value */}
                <button type="submit">Submit</button>
            </form>
        </div>
    );
};

export default AddColorForm;
